{
    "builds": [{
        "src": "admin_panel/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "admin_panel/wsgi.py"
        }
    ]
}
